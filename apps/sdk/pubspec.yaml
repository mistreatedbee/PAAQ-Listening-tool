name: paaq_listening
description: PAAQ Listening Platform SDK for Flutter — automatic event tracking, error capture, session management and performance monitoring.
version: 0.1.0
homepage: https://paaq.io

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
